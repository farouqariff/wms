from django.db import models
from django.contrib.auth.models import Group, Permission

# This module provides API access to Django's built-in Group (roles) and Permission models
# No custom models needed - we use Django's auth system directly
